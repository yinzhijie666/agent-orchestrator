# Release Notes — v1.0.0

**生成时间**: 2026-06-08
**Skill**: document-release

---

## 变更摘要

### 新增功能
- 三层 Agent 路由：Kimi(plan) → DeepSeek(build) → Zen(query)
- AutoExecutor + AutoDispatcher 自动执行 P0/P1 技能
- WebSocket 实时仪表盘
- checkpoints + milestones 里程碑机制
- HTTP bridge 支持事件透传

### 修复
- `opencode-server.js:isHealthy()` 增加 signalCode/killed 检测，修复 SIGKILL 后无法检测进程死亡
- `auto-dispatcher-flow.test.js:D4` 超时测试匹配实际返回值（catch → return）

### 测试
- 325 测试 / 46 文件 / 309 pass / 16 skip / 0 fail
- 新增 E2E full flow 测试

### 文档
- `docs/superpowers/` 技能产物体系建立
- `.gstack/` 报告目录结构标准化

## 已知问题
- D2 (opencode serve) 路径因 upstream Session not found bug 当前为 no-op
- rate-limiter 缺少 Retry-After 响应头

## 依赖
- opencode >= 1.15.0
- bun >= 1.0.0
- Playwright (for browser skills)
